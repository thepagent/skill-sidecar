apiVersion: v1
kind: Service
metadata:
  name: {{ include "skill-sidecar.fullname" . }}
spec:
  selector:
    app: {{ include "skill-sidecar.fullname" . }}
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
